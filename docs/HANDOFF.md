# Current handoff

Snapshot date: 2026-09-09.

## Blog platform upgrade — 2026-09-18

The blog now sanitizes public and preview content on read, repairs the legacy nested-code-block structure, adds deterministic heading anchors, and derives a bounded outline and FAQ candidates from the final safe HTML. The editor adds an explicit paragraph escape, genuine code-block toggle, tables, horizontal rules, HTML-source mode, normalized image insertion, and content-health feedback. Public posts add desktop/mobile contents navigation, reading progress, meaningful updated dates, copied-link feedback, Breadcrumb structured data, and FAQ structured data when the article has qualifying question/answer pairs.

Deployment needs the matching PHP code and Vite bundle. There is no database migration and no new worker. Existing stored content is not bulk-mutated: it is repaired during rendering, then normalized durably the next time an editor saves it under the existing revision system. Verify one legacy rich article, one genuine code sample, a narrow-screen comparison table, heading anchor navigation, and the JSON-LD graphs after deployment.

## Repository state at documentation creation

- Working directory: `/Users/macbookair/Documents/Github-Projects/wisperbot`
- Active branch: `main`
- Baseline commit: `53b7bd5 Clarify published post delete controls`
- One pre-existing, uncommitted user change was present in `resources/js/Pages/Social/Posts/Index.jsx`. It was intentionally not modified by the documentation work.

Always run `git status --short` and inspect recent history before starting; this snapshot will naturally become stale.

## Current focus

2026-09-20 LinkedIn Company Pages (local, uncommitted): LinkedIn previously connected only the member's own profile (`w_member_social`, author `urn:li:person`). Checked the live developer console for app “Wisperbot” (client id 78ypy3kvocwvsg, company verified, callback `https://wisperbot.com/app/social/accounts/callback/linkedin`): its Request access button for Community Management API is disabled because “this API product requires that it be the only product on the application”, and that app already has Sign In + Share. So Company Pages need a SECOND LinkedIn app; the client is creating it. Implementation: optional `pages_client_id`/`pages_client_secret` on `oauth_linkedin`, `?target=pages` on the connect route, variant carried in the OAuth state, Pages read from `organizationAcls` (no member profile on that flow), target picker at `client.social.accounts.linkedin.select` (pending data in session `linkedin_pending_connection`), `meta.actor_type` drives the author URN, and `RefreshSocialTokensJob` re-checks each row, refreshes with the right app's keys and copies the rotated token to siblings. Tests: `tests/Feature/Social/LinkedInCompanyPageTest.php` (8). Never exercised against the live LinkedIn API: it needs the approved second app, so HTTP is faked.

2026-09-20 in-app delete confirmation (local, uncommitted): a Knowledge Base source "would not delete" because Chrome suppressed `window.confirm()` after three quick deletes (server logs showed the earlier deletes and no request for the fourth). All 39 delete-type confirmations now use `await confirmDialog({ message, confirmLabel })` from `resources/js/Components/ConfirmDialog.jsx` (Headless UI dialog, Cancel focused first, Escape/backdrop cancel; host mounted in `app.jsx`). Tests: `resources/js/__tests__/confirmDialog.test.jsx` (includes a guard against native `confirm()` on delete buttons). 13 non-delete native confirmations remain. Not checked in a live browser (in-app browser not logged in; Claude in Chrome not connected).

2026-09-20 widget Appearance save fix (local, uncommitted): `Pages/Chat/Widgets/Settings.jsx` and `Create.jsx` post multipart data with `queryStringArrayFormat: 'indices'` so AI schedule windows reach PHP intact; `Partials/ChatWidgetForm.jsx` merges `usePage().props.errors` (router.post puts errors there) and tracks its own saving state. Regression test: `resources/js/__tests__/widgetSettingsPayload.test.js`. Reproduced by replaying the page's exact FormData against the local widget: brackets → "Enter a valid time", indices → saved. Other `forceFormData` forms sending arrays of objects would need the same option. Multipart data also omits empty arrays, so `ChatWidgetController::validated()` treats missing `allowed_domains`/`prechat_fields` as empty (both forms submit the full form); covered by `tests/Feature/Inbox/ChatWidgetSettingsSaveTest.php`.

2026-09-20 `oris` adopted `main` (merge `09968fa`) and is the forward branch. The full suite now passes: 913 backend, 100 frontend. Clean `main` alone had 80 failing tests; nearly all were stale tests (license server calls, Developer Tools add-on gate, numeric ids where routes use uuids, `withoutMiddleware()` hiding route binding, reliance on the developer's `.env`). `tests/TestCase.php` now disables license verification for every test (license behaviour stays covered by `tests/Feature/License`) and provides `enableDeveloperTools()`; `tests/Concerns/UsesDeveloperApi` enables the add-on for Developer API test classes. Real fixes: exact-id media cache match in `MessageMediaResolver::cachedPath()`, Meta webhook callback from `APP_URL` in `InboxSetupController`, `User` email-inbox preference default/fillable. Media tests now fake the public disk; `storage/app/public/message-media/1.jpg` on dev machines may be a leftover from older test runs.

2026-09-19 Smart Bot starter questions (local, uncommitted): new `StarterQuestions` service, `ai_chatbots.starter_questions_enabled`/`starter_questions` (migration `2026_09_19_000300_…`, run locally), settings editor in `Pages/AI/Chatbots/Index.jsx`, runner first step in `run()`/`runForApi()`, synchronous webchat send in `AutoReplyListener` for a match, `config.starter_questions` labels in `ChatWidget::publicConfig()`, widget list under the welcome message, `answer_origin=starter_question` allowlisted. `AutoReplyListener::HANDOVER_PHRASES` is now a public class constant (was a namespaced file constant) so save validation can reuse it. Tests: `SmartBotStarterQuestionsTest` (8), five widget vitest cases, API field assertions. The widget also polls once after every successful send, so a starter answer appears at once without realtime (local check: typed match rendered in ~110 ms, no credit ledger entry; a following normal question was answered by AI from the KB for 1 credit). The Flutter SDK team needs to render the list from the session config (contract in `CHAT_REPLY_OPTIONS.md`).

2026-09-19 widget: the “Live Chat!” launcher preview now waits five minutes before its first appearance and repeats at most every five minutes (was 20 seconds and every 20 seconds). Constants are `INVITE_DELAY_MS`/`INVITE_VISIBLE_MS` in `public/widget/wisperbot-chat-widget.js`, covered by a fake-timer vitest. Deploy the widget file.

2026-09-19 widget chrome (local, uncommitted): the launcher is hidden while the chat is open and the panel extends into that space (desktop and mobile); closing returns focus to the launcher. The header “•••” was a decorative non-interactive span and is now an accessible options menu (Mute sound / Turn sound on, persisted per widget; Talk to an agent when a handoff can be requested). While verifying, allowed domains saved as `localhost:8000` stopped the widget from loading, because the loader and API compared the entry with the port against a port-less host. Both now strip scheme/path/port/whitespace (new `WidgetRealtimeTest` case). Tests: 84 frontend (2 new shell tests), widget/Inbox suites with the known pre-existing Inbox failures only, Pint clean. Deploy `public/widget/wisperbot-chat-widget.js` plus the two widget controllers.

2026-09-19 product change (local, uncommitted): the website widget now presents a matched Knowledge Base video as a “▶ See Tutorial →” link below the reply text, opening the YouTube/Vimeo page (or MP4) in a new tab, instead of the in-chat player introduced earlier the same day. When a video is attached is unchanged. Prompt wording now describes the link. Customer sites no longer need frame/media CSP permissions. Widget vitest cases updated (link after text, new tab, `noopener noreferrer`, accessible name, no iframe/video), and all AI tests pass. Live widget check: install flow, question turn without link, solution turn with See Tutorial linking the YouTube watch page. Deploy `public/widget/wisperbot-chat-widget.js` with the backend. The Customer Chat SDK should adopt the same link presentation, and the agent Inbox card is unchanged.

2026-09-19 Smart Bot reliability and languages (local, uncommitted; see `docs/KNOWLEDGE_BASE_SMART_BOT.md` → Reliability and languages). Causes of the reported "same question sometimes fails / no video / other languages fail": website-chat replies ran inside the visitor's HTTP request, where one hit PHP's 30-second limit and the reply was lost; the model randomly omitted JSON keys (`grounded`, `show_video`), and a single bad sample became a handoff; romanized Bengali scored 0.21–0.28 against the English KB; the video was only offered at rank ≥ 0.72, which button-press follow-up turns rarely reach; and the per-minute managed budget also counted internal steps. Fixes: `ProcessWebchatAiReplyJob` on `ai`; OpenAI strict structured outputs with a JSON-mode fallback; one validator retry under the same reservation; `englishSearchQuery()` added to search (0-credit `kb_search_translation`); videos offered at the answer threshold, shown on flag, link, or any-language mention, with every linked step set given to the model; Roman Bangla social phrases; replies keep the customer's script; `composer dev` listens on all queues. The Telzen test workspace exhausted its 100 monthly credits during verification; +100 local `adjustment_credits` were added to period 2 (reversible). A duplicate `MessageReceived` listener registration (explicit plus discovery) was found and filed as a separate task. Follow-up: near-duplicate passages (overlap ≥ 0.75) now collapse, so an older re-upload no longer displaces the copy that links the video. Grounded statement-plus-question clarifications are accepted. Videos are offered from any evidence passage in any mode but withheld on qualifying turns (choices plus text after the question). Real-call reliability runs (fresh conversations, clicking the bot's own first choice): English, Bengali, and Roman Bangla had no handoffs, with the video on every solution turn in the final runs.

2026-09-19 Smart Bot reply style (local, uncommitted; product decision "facts strict, wording free"; see `docs/KNOWLEDGE_BASE_SMART_BOT.md` → Reply style). Changes: natural wording by default, with a new per-bot `kb_exact_wording` switch (migration `2026_09_19_000200`, client form toggle, API field). Scripts guide the flow instead of being copied; script markers and video links never reach customers (a pasted matched-video link becomes the player). Question-only follow-ups are accepted even when marked ungrounded. Clarification mode may answer when the passages clearly do. A deterministic figure check rejects any price, size, duration, or other figure missing from the evidence, including in choices. General guidance may not name plans or sizes. Statement-style offers ("feel free to ask", "…, let me know!", "If you need help…") also qualify for the zero-credit goodbye. Live Telzen replay: the price question answers "$1.20 or ৳128" (was a handoff), "help me choose a plan" asks a follow-up with choices (was a handoff), "No thanks" gives a branded goodbye, the install flow keeps the question → steps + player, and invented "5GB/10GB" plan claims are now blocked. Tests: 11 `SmartBotReplyStyleTest` cases (the figure and choice guards fail when disabled), router and settings/API cases; 341 related tests pass, and the 8 failures, plus 31–32 in `tests/Feature/Api`, reproduce on untouched code; 82 frontend tests; build; Pint; PHPStan none new. The Telzen bot is configured with "Offer human help immediately", which is why unsupported turns hand off without clarifying first.

2026-09-19 conversational closing (local, uncommitted): "No" after the bot's own "anything else?" offer produced the handoff fallback, because the turn reached strict KB grounding and the model's goodbye was rejected as ungrounded. `BusinessAwareTurnRouter::offerReplyResult()` now answers declines, acceptances, and clear closings deterministically, before retrieval and independent of the business-aware flag (router unit cases plus a runner test with the flag off and no model call). The live widget did not produce an "anything else?" offer during verification, so this is test-verified only. Open design issue (awaiting product decision): the strict validator rejects any `grounded:false` output, including legitimate follow-up questions ("What type of data usage do you expect?") and closings, and knowledge-only bots turn each rejection into a handoff. Replies also copy scripted KB lines verbatim, including raw video URLs.

2026-09-19 customer-facing replies (local, uncommitted): per product decision, sources are never shown to customers. `ChatbotRunner` no longer appends "Sources:" links to reply text (this also covers WhatsApp/Messenger/email), and `WidgetPayloadBuilder` always sends `citations: []` publicly, while citations stay on the stored message for staff and in the AI chat API. Clickability: widget buttons already worked when present; the real gap was closing yes/no offers ("Would you like help with anything else?") arriving without buttons. The model is now told such offers need choices, and server recovery covers them, with exclusions evaluated on the question sentence only. Live replay (typing plus clicking choices, no reload): every question carried clickable choices, the research plans answer had no sources, and the video appeared only on the install steps. Tests: widget payload/poll assertions updated, a new runner test proves a research answer reaches customers without source links while keeping staff citations, and new recovery cases were added. 326 related tests pass; the 8 failures reproduce on untouched code; Pint clean; PHPStan on the changed files 20 → 17, none new. Native SDKs should stop rendering citations if they do.

2026-09-19 Knowledge Base source authority on `oris` (local, uncommitted): diagnosed Telzen answers copying outdated website FAQ ("eSIM only, no physical SIM") over the client's own document. Causes were platform-wide: the saved Authoritative/Priority settings were never used by retrieval, the correct KB business profile only reached general-guidance turns, keyword-only candidates entered at semantic score 0, duplicate uploads filled 2 of only 3 evidence slots, and the prompt said "prefer the highest-ranked passage". Fixed in both retrieval paths (see `docs/KNOWLEDGE_BASE_SMART_BOT.md` → Source authority). Also: availability questions count as explicit intent, default window 5 passages / 1,600 tokens, replies up to 70 words with a 320-token budget. Live widget on Telzen KB: "how telzen works" and "what is telzen" now answer from the .docx (eSIM and physical SIM); "do you sell physical sim" went from handoff fallback to a grounded yes with next-step choices; the eSIM install/video flow still passes. Verification: 7 new `KnowledgeSourceAuthorityTest` cases, all failing on the prior code; the 2 stale `ChatbotRunnerTest` fixtures repaired (now pass); 321 related tests pass, and the 8 remaining failures reproduce on untouched code (campaign limit, Meta operations rate limit, Inbox labels/HEIC/timestamps); changed-file Pint; PHPStan on the changed services 19 → 17 with none new. No migration, SDK, or frontend bundle change. Deploy the backend, refresh the config cache, and restart `ai`/message workers. Telzen data still needs client action: remove duplicate document 5, correct the telzen.net FAQ, and consider removing the Terms/Privacy pages as answer sources.

2026-09-19 Smart Bot video-turn fixes on `oris` (local, uncommitted): a matched KB video is now shown only on the reply that delivers its solution, never on follow-up/clarification questions or fallback replies (model `show_video` flag plus deterministic question-only guard; see `docs/KNOWLEDGE_BASE_SMART_BOT.md` → Video resources). Discovered videos use the provider's oEmbed title instead of the source document name, with legacy document-name titles suppressed at answer time. Widget and web Inbox video stages now hold 16:9. Follow-up the same day: per product decision, the website widget now shows guide text first and the video below it as the provider's native embedded player, with no title bar and no "Open video"/outbound link (2 new widget vitest cases); the Inbox keeps its agent card. The Customer Chat SDK should adopt the same layout. Second follow-up: a plans reply built from website research still carried the eSIM-activation video, because the video could come from any ranked candidate, a missing `show_video` flag defaulted to showing, and the model was never told what the video was. Now only passages sent to the model can supply a video, the prompt names the video title and the steps it accompanies, and only an explicit `show_video: true` shows it (4 new `SmartBotVideoTurnTest` cases; the passage-restriction case fails in both retrieval modes without the check). Live replay: install steps → video; "Yes, show me plans" → no video. The Telzen .docx links the same activation video after five different step sets (buy, install, physical SIM, roaming), so the buy-steps reply still shows it by the client's own authoring. Live widget check on the Telzen KB: "How to install eSIM?" → question with no video; "Yes" → steps with the "How to Activate Telzen eSIM in Minutes…" card at 1.778 ratio. Verification: 6 new `SmartBotVideoTurnTest` cases, 2 new `VideoResourceServiceTest` cases, 113 related AI/KB/widget tests pass (the 2 `ChatbotRunnerTest` failures reproduce on untouched code: their fixtures predate the published/approved/ready retrieval gates), all 80 frontend tests, changed-file Pint, production build. No migration, SDK contract change, or version bump. Deploy matching backend, `public/widget/wisperbot-chat-widget.js`, and Vite bundle; restart `ai`/message workers; reindex existing video-bearing sources to fetch real titles. Native SDKs should render an untitled video with the provider name.

2026-09-19 experimental `oris` live product pricing: URL/sitemap page indexing can detect Schema.org Product/Offer data into workspace/KB-scoped product and offer records kept outside embeddings. Opted-in Smart Bots resolve price/stock intent before generation, require a published approved source, clarify ambiguous products/variants with compatible CTAs, prefer fresh same-host connected-commerce data, and return deterministic zero-credit text with safe citations and additive `product_facts`. Stale sources are reverified; failure never exposes stale or guessed prices. The feature defaults off through `KB_LIVE_PRODUCT_FACTS_ENABLED`; migration `2026_09_19_000100_create_live_kb_products.php`, scheduler, `ai`/message worker restart, matching frontend, and controlled source refresh are required. This work remains local and uncommitted pending full verification.

2026-09-19 experimental `oris` Smart Bot configuration simplification: raw max-context, confidence, token-budget, and video-threshold controls are removed from the client form. `SmartBotRetrievalPolicy` now supplies bounded private-chat values from `KB_MAX_CONTEXT_CHUNKS`, `KB_RETRIEVAL_MATCH_THRESHOLD`, `KB_MAX_CONTEXT_TOKENS`, and `KB_VIDEO_MATCH_THRESHOLD`; public comments derive a smaller, never-less-strict policy. Legacy database/API fields remain for compatibility, new bots receive current policy snapshots, and client updates ignore submitted tuning fields. No migration or SDK change is required; deployment needs matching backend/frontend, config-cache refresh, and AI/message worker restart.

2026-09-18 experimental admin/client product-shell redesign on `oris`: shared navigation is now a compact 236px light/dark sidebar with orange active rail, work-frequency client grouping, permission-preserving admin grouping, session-local group/scroll state, and a topbar mobile drawer instead of floating navigation. Existing admin command search is visible in the topbar. Shared controls/surfaces and both dashboards follow the approved operational design, while the client dashboard links directly to existing Inbox, Channel Setup, Smart Bots, Knowledge Bases, and Team journeys. Auth uses matching light/dark surfaces. No feature, route, permission, API, schema, SDK, worker, version, commit, promotion, or deployment is included.

2026-09-18 hero Agent App chooser on `oris`: the homepage secondary action now opens a minimal anchored iOS/Android dropdown instead of scrolling to Product Tour or showing a large modal. It uses the official WisperBot App Store and Google Play listings, closes on selection/Escape/outside click, and keeps SDK ownership distinct. Frontend/documentation/default-copy only; no migration, mobile release, production deploy, or version bump.

2026-09-18 auth redesign on `oris`: Sign in and Register now share the product-led graphite/warm-white auth shell. The old unverified social-proof metrics and quantified outcome claims were removed. Direct registration still starts on the enabled Free plan, and paid Pricing intent still continues to checkout after workspace creation. This is a frontend/documentation change with no migration, worker restart, SDK release, production deployment, or version bump. Deploy the matching Vite bundle when promoted.

2026-09-18 streamlined initial-plan signup on `oris` (supersedes the plan-grid registration UI): registration is again a compact account form. Direct password, social OAuth, and Firebase signups transactionally activate the first enabled Free plan by sort order. Public Pricing carries a paid plan/cycle as intent; registration shows only a compact summary, creates the workspace on Free, then sends the customer to secure checkout. Abandoned checkout retains usable Free access. The defensive no-plan gate, same-client teammate access, server-derived onboarding milestone, and Free cancellation protection remain. The 30 focused backend tests (100 assertions), 76 frontend tests, focused PHPStan, changed-file Pint/ESLint, production build, `git diff --check`, and live Chrome direct/paid-intent checks pass. The full backend suite has 716 passes and the same 81 unrelated baseline failures across admin/API/inbox/marketing/provider fixtures; none are in this signup journey. No schema migration, native app/API contract, worker restart, production deploy, version bump, commit, or promotion is included.

2026-09-18 marketing icon-system follow-up on `oris`: removed the marketing demo's letter/Unicode provider map and centralized provider presentation through `ProviderBrandIcon`. The homepage provider ribbon, integration directory, capability surfaces, and product illustrations now use recognizable local provider SVGs; Gmail and Microsoft use multicolour marks, and Telegram/marketplace artwork is retained. Non-brand concepts and actions use Lucide, and the shared legacy integration tile now falls back to a Lucide plug rather than a misleading initial. All 73 frontend tests, targeted ESLint, the production build, and `git diff --check` pass. Chrome verified all 20 integration cards contain SVG artwork, the five non-brand concepts use Lucide, and no provider card is empty. Frontend-only, local, uncommitted; no migration, API, SDK, worker, version, or production change.

2026-09-18 latest hero direction on `oris` (supersedes the earlier light-background/floating-icon experiments): the homepage header and hero now form one dark graphite stage, and the sticky header returns to the standard white treatment once the hero passes behind it. `HeroLaunch` performs a short decorative brand reveal; `HeroHeadline` staggers the copy and keeps one rotating SVG signal between “Every” and “customer,” using the real local WhatsApp, Facebook, Instagram, Messenger, Telegram, LinkedIn, X, TikTok, and YouTube marks instead of generic email/chat glyphs; the surrounding icon tiles remain removed. `HeroBackdrop` combines a dotted SVG network with orange/amber/evergreen light and requestAnimationFrame-throttled pointer tilt/local light. It is clipped, `aria-hidden`, and noninteractive; coarse pointers stay static, reduced motion removes the launch/rotation, and manual pause stops pointer response. Client-authored non-default headline text remains supported. All 71 frontend tests, targeted ESLint, the production build, and `git diff --check` pass. Chrome confirms dark-to-light header switching at the hero boundary and all nine real icon sources. Frontend-only, local, uncommitted; no migration, SDK change, version bump, or production deployment.

2026-09-18 experimental `oris` public-website revamp: replaced the homepage-only redesign with a complete product-led site. Shared mega navigation and mobile slide-over lead to 14 new product/solution/channel/developer pages; Pricing, Integrations, FAQ, About, Contact, Use Cases, Blog/article, and CMS presentation use the same system. The homepage includes an interactive product tour, scroll-linked customer lifecycle, code-native inbox/AI/chat/mobile/workflow/social/commerce illustrations, capability matrix, solution selector, trust, free-plan entry, guides, and FAQs. Demonstrations are explicitly illustrative, never claimed as real customer proof. Site Content now manages safe global copy, categorized FAQs, SEO, and official iOS/Android/pub.dev/developer URLs. Missing downloads are hidden. Social-comment claims follow the rollout flag. The local free plan currently disables branding removal; marketing respects this rather than changing entitlements. `docs/DESIGN.md` owns the durable full-site plan; `DESIGNSYSTEM.md` owns UI rules. No production deployment, migration, worker restart, SDK change, application-version bump, commit, or promotion is included. Deploy matching backend/config/routes and locally built Vite assets together after approval.

Marketing verification: 25 focused backend tests (346 assertions), all 64 frontend tests, production build, changed-component ESLint, changed-file Pint, and focused analysis of `MarketingCatalog`/both landing controllers pass. Full `composer analyse` still reports 716 repository findings; no suppressions were added. The normal build retains existing oversized app/BulkImport chunk warnings. Chrome checks cover mega-menu Escape/focus, mobile navigation, product-tab keyboard interaction, real integration filtering, dedicated product pages, animation pause/static visibility, and responsive widths 320/390/768/1024/1440. The marketing content fits its narrow containers; browser viewport emulation reports an additional root scroll-width discrepancy at 320px despite body/marketing width 320px, so a physical narrow-device check remains advisable. No Lighthouse score or native-device certification is claimed. Local Laravel and Vite remain available at `http://127.0.0.1:8000` / port 5173. Before launch: provide official download/package URLs, decide free white-label entitlement, approve translations/copy, and run production-bundle accessibility/performance checks.

2026-09-16 durable Smart Bot/KB decision record: `docs/KNOWLEDGE_BASE_SMART_BOT.md` is now the authoritative guide for private business-aware routing, semantic retrieval, approved-source research, grounding, credits, chatbot teammate identity, feature flags, rollout, and SDK compatibility. `AGENTS.md`, the documentation index, Product Decisions, and the quick-reply contract link to it. Update the guide whenever those behaviors change.

2026-09-16 experimental `oris` team identity follow-up: client administrators can upload, replace, or remove teammate JPG/PNG/WebP photos from Add/Edit Team. Stored paths remain private and all inbox/realtime/mobile/widget contracts resolve them through `User::avatarUrl()`. Public widget presence now includes only teammates active under the workspace schedule; after join, human replies and joined state use the actual teammate photo (initials fallback), while Smart Bot messages retain company branding. No migration or version bump is required. Deploy matching backend/frontend/widget assets; native SDKs may optionally adopt the additive `agent_avatar_url` and `avatar_url` fields.

2026-09-16 experimental `oris` inbox delivery resilience: local Brave-to-Chrome widget testing proved inbound messages were stored immediately in the correct workspace, while Chrome remained stale because local Pusher/Reverb was intentionally unconfigured. The visible Omni inbox now reconciles every four seconds without Echo and every 30 seconds with Echo, in addition to focus/visibility refreshes. Reconciliation keeps existing safe-refresh guards and workspace-scoped server queries. No migration or worker restart is required; deploy a rebuilt frontend bundle if this experimental change is promoted.

2026-09-16 experimental `oris` Knowledge Base URL normalisation: web and developer API source creation now preserve `original_source_ref`, normalise natural domain input to HTTPS, and use one SSRF-guarded resolver for page and sitemap fetching. The resolver tests apex/`www` variants, upgrades same-site HTTP redirect targets without requesting HTTP, detects true cycles, rejects cross-site redirects, and stores `canonical_url`; the source table shows the resolved secure address when it differs. Migration `2026_09_16_000100_add_canonical_urls_to_ai_kb_documents.php` is required. A live resolver check canonicalised `telzen.net` to `https://www.telzen.net/` with HTTP 200. Verification passes 34 focused backend tests (95 assertions), all 55 frontend tests, the production Vite build, and changed-file Pint. This remains experimental on `oris` and is not approved for promotion or deployment.

2026-09-19 outbound provider image normalization (local, not deployed): staff web and mobile attachment sends now inspect actual bytes and pass JPEG/PNG unchanged. HEIC/HEIF/WebP/GIF become temporary JPEGs for WhatsApp and persisted provider-safe JPEG URLs for Messenger, Instagram and Telegram; captions and existing document/audio behavior are unchanged. WhatsApp media upload is bound to the conversation's phone number. Stale `.heic` filenames containing JPEG bytes are treated as JPEG. Meta asynchronous delivery errors are retained in `messages.error_json`. Converter binaries are operator-configurable; ExonHost requires `HEIF_CONVERT_BINARY=/opt/alt/libheif/bin/heif-convert`. No migration, frontend/app/SDK change or historical retry is included.

2026-09-19 WhatsApp sender fallback (local, not deployed): inbound Cloud API messages now normalize `messages[].from`, fall back to the standard `contacts[].wa_id` when it is empty, and reject payloads with neither identifier instead of creating an invalid `+` contact. Existing valid sender routing, workspace scoping, consent handling, conversation ownership and stored message payloads are unchanged. Existing malformed contacts are not guessed or rewritten.

2026-09-17 conversation activity timeline (local, not deployed): staff timelines now retain successful join, takeover, leave, assignment, unassignment, reopen, pending, snoozed, and resolution as durable system/event records with actor/subject snapshots where applicable under the existing conversation lock and transaction. Staff web, Email MasterBox, and staff Flutter render centered activity rows. Widget/customer-SDK history exposes only joined/resolved activity; takeover is redacted to the new agent joining, and public payloads contain only `type` and `actor_name` with a `role=agent` body fallback. Activity is excluded from message previews/order/unread/SLA, provider delivery, AI, automation, webhook, notifications, and Developer API history. Deployment order is migration, backend/web/widget, cache plus worker/Reverb restart, staff app release, then the separately owned customer SDK package and host-app rebuild. No historical backfill is performed.

2026-09-17 Master Email Inbox alert preference (local, not deployed): web users can independently disable all database/realtime/browser/mobile-push alerts produced by new inbound email while mailbox synchronization, email unread state, replies, existing notification history, and non-email channel alerts remain unchanged. Existing users default On through additive migration `2026_09_17_000100_add_email_inbox_notifications_to_users.php`. Mobile login and `GET /api/v1/auth/me` expose `email_inbox_notifications_enabled`; authenticated `PUT /api/v1/notifications/preferences/email-inbox` accepts `{enabled: boolean}`. The native app repository was intentionally left read-only for the app team. Deployment requires the migration, matching backend/web bundle, cache refresh, and queue restart. Verification: all 58 frontend tests, focused changed-file ESLint, the production Vite build, and `git diff --check` pass. Laravel tests remain unverified on this Windows host because PHP is unavailable.

2026-09-17 resolved-conversation reopening (local, not deployed): genuine inbound customer messages now reuse and atomically reopen the existing conversation as Open/Unassigned before automations, AI, notifications, and realtime listeners run. The transition clears resolution, ownership, handoff, AI-pause, first-response, and unanswered-reminder state while retaining transcript/labels; additive realtime metadata lets the web Resolved and Unassigned views reconcile without breaking existing consumers. WhatsApp history/business-app echoes, outbound traffic, and status callbacks do not reopen threads. No migration, new dependency, or native-app change is required; deploy matching backend/frontend, rebuild Vite assets, and restart inbound/default workers. Verification: 79 focused backend tests (400 assertions), all 56 frontend tests, changed-file PHP syntax/Pint, the production Vite build, and `git diff --check` pass. Focused PHPStan reports 44 existing typing findings in the touched legacy files; ESLint reports 13 existing errors and 16 warnings in the legacy Inbox components. The reopen changes add no suppression or baseline entries, and their focused frontend regression test passes.

2026-09-15 Omni inbox list alignment (local, not deployed): web and mobile conversation lists should treat **All** as message-bearing Omni threads, while no-message website visitor sessions remain in Live Visitors. The mobile `/api/v1/mobile/conversations` default list now matches web semantics by excluding empty visitor sessions and not narrowing `folder=all` to open-only threads; mobile inbox counts follow the same real-thread rule. The web Omni list and conversation detail sidebar keep the existing 30-row backend pagination but auto-load additional pages with infinite scroll. Deploy matching backend/frontend and verify native staff app list/count behavior.

2026-09-14 mobile API rate-limit increase (local, not deployed): dedicated mobile, authenticated mobile auth/profile, and mobile broadcasting authorization now use `mobile-api`, default 300/minute/user (`MOBILE_API_RATE_LIMIT_PER_MINUTE`), separate from generic/developer `api` at the upstream 120/minute. Login and stricter action-specific limits are unchanged. Structured 429 responses preserve `Retry-After` and add `code`/`retry_after`; all devices belonging to a user share the budget. Production/mobile network traces have not established which requests caused the reported 429. Deployment requires matching backend/config/routes and rebuilding config/route caches; no migration. Added `MobileRateLimitTest` for the boundary/recovery, budget isolation, user/device identity, operator configuration, and route guards. All 52 frontend tests and `git diff --check` pass; Laravel tests, Pint, and PHPStan remain unverified because PHP is unavailable on this machine. Native request-loop/backoff behavior still requires app-team verification.

2026-09-14 mobile inbox layout fix (local): replaced the bottom-right floating menu in `InboxLayout` with normal-flow mobile top navigation, including an optional return-to-inbox link. Chat detail hides desktop filter/list columns below `lg`; the Omni list uses full mobile width with collapsible filters and a retained new-conversation action. Dynamic viewport sizing and shrinkable flex children protect composer space; the send control has a 44px touch target. Email MasterBox shares the new top navigation. No backend, mobile API, or SDK contract changes; deployment needs the updated production frontend bundle. Verification: all 52 frontend tests, production Vite build, and `git diff --check` pass; the build retains existing large-chunk warnings. Physical-device keyboard behavior still requires device verification.

2026-09-12 workspace-segment AI answering (local, not deployed): Channel Setup owns one Omni Off/Always on/Scheduled policy shared by every WhatsApp, Messenger, Instagram DM, Telegram Business, and eBay account; Email Setup owns one independent policy shared by every mailbox. Both controls render before accounts are connected. Exact active hours are evaluated on every inbound, human control pauses AI until resolution, and account eligibility cutoffs prevent historical imports. Conflicting legacy account policies migrate Off with Needs setup. Website Appearance, Social Comments, Amazon, and SMS are unchanged. Deployment requires `2026_09_12_000300_add_channel_ai_answering_policy.php` followed by `2026_09_12_000400_replace_account_ai_policy_with_segment_policy.php`, matching backend/frontend, cache refresh, and `ai`/`whatsapp`/`default` worker restart. Verification passes 32 focused Laravel tests (108 assertions), all 44 frontend tests, the production Vite build, changed-file Pint, focused new-service PHPStan, and browser checks on both empty-account setup pages. Repository-wide Pint and PHPStan still report the documented pre-existing baseline (722 PHPStan findings).

2026-09-12 Smart Bot selection simplification: removed the redundant per-bot Active switch and all runtime/list filtering based on the legacy `ai_chatbots.enabled` flag. Explicit assignment to a website widget, channel, automation, or social account is the only activation state. Appearance lists every workspace bot and retains a clear fail-closed warning only when the selected bot is missing/deleted. The mobile/developer API keeps `enabled: true` for backward compatibility.

2026-09-12 team availability and chat ownership: workspace members may have timezone-aware weekly notification coverage with split, all-day, and overnight windows. New-message/handoff alerts route to available teammates; assignment and account-critical alerts remain unaffected. Omni-channel conversations now separate pre-assignment from an atomic joined owner, require the joined owner for human replies, expose web/mobile join/leave/takeover APIs, broadcast ownership changes, and reset ownership/handoff state on resolution. Website customers see waiting and joined-agent states without sending join messages to external providers. Appearance now uses Permanent/Scheduled AI active hours and converts legacy inside/outside schedules exactly. Deployment requires `2026_09_12_000100_add_availability_and_join_state.php`, matching backend/frontend/widget assets, cache refresh, and worker restart. The native customer SDK must adopt the additive handoff fields separately. Verification passes all 40 frontend tests, the production Vite build, changed-file PHP syntax checks, changed-file Pint, and 40 focused Laravel tests with 216 assertions on the cPanel PHP 8.2 runtime. Repository-wide Pint retains unrelated pre-existing formatting debt.

2026-09-11 automation queue normalization: `ExecuteAutomationRunJob` now owns the canonical singular `automation` queue, and all API, web, event, retry, sub-flow, and delayed-resume dispatch paths rely on that assignment. This fixes Developer API runs previously stranded on `automations`. Deployment requires a normal worker restart and a one-time `automations` queue drain for jobs created before the fix. No migration, frontend build, or API contract change is required. Verification: dispatch-source scans, `git diff --check`, all 37 frontend tests, and the production Vite build pass. Laravel tests, Pint, and PHPStan could not run in this checkout because PHP, Composer, and `vendor/` are unavailable.

2026-09-09 Website Widget AI scheduling: Appearance now offers an optional **Schedule AI answering** control beneath the existing Smart Bot selector. Clients define weekly office hours, choose **Outside office hours** (recommended) or **During office hours**, and select an IANA timezone. `chat_widgets.ai_schedule_json` is evaluated server-side for each inbound webchat message and for the public handoff state; invalid enabled schedules fail closed. Scheduling is off for existing widgets, so current continuous behavior is preserved. The local migration `2026_09_09_000200_add_ai_schedule_to_chat_widgets.php` has been applied. Production requires that migration plus matching backend/frontend; no queue or mobile SDK contract change is required. Verification: 49 focused backend tests (245 assertions), all 37 frontend tests, production Vite build, ESLint for the changed component, and changed-file Pint pass. The full suite is 630 passes/74 known unrelated failures, retaining the exact failure baseline; repository PHPStan remains at the same 681 pre-existing findings.

2026-09-09 Smart Bot relevance hardening: `unsupported_answer_action` now controls answer scope in every runtime mode rather than only when guarded publishing is enabled. Bots with an assigned Knowledge Base default to knowledge-only: low-relevance retrieval returns the configured fallback before chat generation, strict provider output requires `grounded: true`, rejected output refunds its reservation, and explicit `general` mode allows safe general answers while retaining business-fact constraints. The client setting is now a clear **Answer outside the Knowledge Base** switch with a conditional fallback selector; AI API list responses expose the additive scope fields. No database migration is required because the existing enum remains authoritative. Deploy matching backend/frontend and restart AI/message workers. Verification: 82 focused backend tests (380 assertions), all 37 frontend tests, production Vite build, and changed-file Pint pass. The full backend suite is at 623 passes/74 known unrelated failures, preserving the prior 74-failure baseline; repository-wide PHPStan reports 681 pre-existing findings (improved from the previously documented 686 baseline).

2026-09-09 workspace notification isolation: client database/realtime/push notifications now carry their source `workspace_id`; web and Sanctum list/count/read/delete operations scope to the active accessible workspace; shared-workspace owners/members are resolved independently of their current primary workspace. Mobile clients receive `workspace_id` in notification and push payloads and must switch/validate workspace before deep-link navigation. Deployment requires `2026_09_09_000100_scope_notifications_to_workspaces.php`, matching frontend/backend, cache refresh, and queue restart. Legacy client notification rows are assigned to each user's current workspace during migration; Super Admin notifications remain unscoped. Verification: 57 focused backend tests (181 assertions), all 37 frontend tests, production Vite build, changed-file Pint, and focused notification PHPStan pass. The full backend suite is at 620 passes/74 known unrelated failures, preserving the prior 74-failure baseline rather than introducing a new failure.

2026-09-08 branch promotion: the remediation batch below is being committed through Spiderman-v2 → dev. Main remains at the prior release because the documented static-analysis and full-suite gates are still failing. No migration, production deployment or SDK change is included.

2026-09-08 analysis remediation in progress (uncommitted): configured module migration discovery and `casts()` parsing, typed 115 direct model relationships, removed 546 newly obsolete baseline entries and reduced two counts without adding suppressions. Static findings fell from 1,729 to 686. Runtime hardening covers missing resumed Messenger user-token state, sitemap HTML state, private LLM helper dispatch and exhaustive blog bulk-action validation. Focused affected tests pass 38/38; full suite reports 617 passes and 74 failures, not yet baseline-isolated. Main is not cleared for merge. Remaining array/generic contracts and test failures require further work.

2026-09-08 static-analysis follow-up: removed only the 16 baseline entries pointing to deleted Leads/Twitter files, retaining analysis level and unmatched-ignore checks. `composer analyse` now executes and reports 1,000+ application-wide errors. Main remains held pending resolution or an explicitly approved release-gate exception; this cleanup is not a passing analysis result. See `KNOWN_ISSUES.md`.

2026-09-08 branch handoff: local header explicitly uses the requested existing widget, supplies server-signed signed-in identity, and resets identity across Inertia sign-in/out transitions. This override is gated to the local environment; production widget selection is unchanged. Verification: 41 focused backend tests (160 assertions), 35 frontend tests and production build pass. Full static analysis remains blocked by pre-existing missing Leads/Twitter ignore paths. Local widget UI was exercised with simulated multi-industry responses; real local provider/KB and native SDK verification remain outstanding. Mobile SDK repository is excluded. No migration or application version increment is required for these changes.

2026-09-08 durable product clarification: dynamic Smart Bot questions/CTA replies are an existing industry-independent SaaS capability. `AGENTS.md` now routes this work to the explicit product invariants and `docs/CHAT_REPLY_OPTIONS.md`; the guide distinguishes merged server/widget code, uncommitted hardening, unresolved live verification and the app-team-owned SDK release. Do not treat client examples or limited English recovery as the platform design. This follow-up changes documentation only and preserves preceding code edits.

2026-09-08 dynamic-choice clarification: generation instructions explicitly cover arbitrary contextual questions and localized labels, not fixed yes/no or eSIM questions. Tests cover device, installation method, troubleshooting stage, service goals and Bengali labels. Existing generic widget/SDK payload fields remain unchanged; narrow recovery is only a fallback. No SDK code or production settings changed.

2026-09-08 local follow-up: tightened generated closed-question choice instructions and added conservative English Yes/No and Supported/Not supported recovery for omitted structured choices, shared by conversation/API runners. Existing SDK response fields and text send semantics are unchanged; native SDK implementation remains with the app team. No production deployment or historical message backfill is implied. Prior uncommitted Smart Bot terminology edits are preserved.

2026-09-06 commit handoff: `Spiderman-v2` is based on the fetched remote `main` at `96ef51f`, with the pending WisperBot application commit replayed on top. All four production hotfix commits are now ancestors, rather than duplicated feature history. Documentation overlap and a test import were reconciled while preserving the hotfix behavior and feature changes. Scope includes server/web suggested replies, compact CTA styling, pending Social Comments and WhatsApp setup refinements. The separate `wisperbot_chat` Flutter repository is excluded and remains uncommitted; SDK implementation and release belong to the app team. This operation does not push, deploy, or apply migrations. Social Comments still requires its migration and controlled rollout. Older checkout/ancestry notes below are historical snapshots superseded by this entry.

2026-09-06 UI refinement: web widget and Smart Bot Playground suggested replies now use 34px minimum desktop buttons with softer borders, subtle fill and tighter spacing. Touch devices retain 44px targets. This refinement is local only; the separately owned Flutter SDK was not changed in this follow-up. No API or migration changes.

2026-09-06 suggested replies are implemented locally in both this repository and `/Users/macbookair/Documents/Github-Projects/wisperbot_chat` (cloned from `Netro-Systems/wisperbot_chat`, baseline `711f73b`). No commit, push, production deployment, package publication or SQL migration was performed for this feature. See `docs/CHAT_REPLY_OPTIONS.md`. Choices use one normal AI generation, safe structured parsing, readable legacy fallback, widget buttons, read-only inbox labels, and additive mobile payloads. Playground now preserves bounded history and the idempotency header. Flutter prebuilt/headless consumers get typed choices and guarded selection; installed apps need an SDK dependency update and a rebuilt app. Other pending work is preserved.

Suggested-reply verification: 60 focused backend tests (250 assertions), all 35 frontend tests, production Vite build, changed-file Pint and focused new-service PHPStan pass. Flutter `analyze lib test` passes and all 140 non-golden SDK tests pass. Five golden comparisons fail identically on untouched SDK baseline under Flutter stable installed temporarily at `/tmp/wisperbot-flutter`; full SDK analysis also flags the pre-existing missing example `.env` asset. No golden references were overwritten. Browser QA used a clearly labelled local fixture at `http://127.0.0.1:8124` and verified keyboard selection, visible visitor text, disabled old choices and subsequent fixture reply; it did not send real customer messages or call live AI. Production/provider and actual device verification remain rollout checks.

Live follow-up at 20:29 Asia/Dhaka: target KB has 20 indexed sources, 59 embedded chunks, 16 queued/in-progress sources, and 6 unsupported-content failures. Two labelled website chatbot tests received usable AI replies; both latest chat ledger entries succeeded at one credit each, while the earlier failed entry is refunded. The client pool reports 74/100 remaining, zero reserved (other consumption includes KB quality reviews). Background retries remain on the normal `ai` worker; no unrelated jobs were replayed. Do not describe all pages as completed until the remaining queue drains.

2026-09-05 crawler/managed-AI hotfixes deployed independently through remote-main `96ef51f`, production v1.3.42 (preceded by `6e9ea36` and `81e001d`). Runtime OpenAI selected a project-disallowed model while the old admin test used a different allowed model. Defaults now use `gpt-4o-mini`; admin validation exercises configured routine/complex models and embeddings. Empty outputs refund credits. A real authorized website chatbot test returned a useful reply and its ledger charged exactly one credit; prior failed chat attempts were refunded. Root website discovery now checks sitemaps before a potentially stalled homepage and normalizes only the validated www/non-www alias. Qdrant integer payload indexes are created automatically without deleting vectors or disabling strict mode. Production cleanup passes; affected page jobs were retried on `ai`. Some source endpoints return unsupported content or stall and must remain explicit failures, not silently indexed partial HTML. 62 focused tests (190 assertions) and changed-file Pint pass; repository-wide analysis is blocked by pre-existing removed-file ignore paths. No SQL migration or Vite bundle change was needed (locale strings load through `/i18n/{locale}`). Pending unrelated Comments/WhatsApp changes remain local. Local main is behind remote; preserve dirty changes when reconciling the already-deployed patches.

2026-09-05 Qdrant deletion hotfix deployed independently through commit `9a5af91` on remote main, production v1.3.39. Ten focused tests and Pint pass; a production HTTP-faked missing-collection check passes without touching real vectors/documents, and `/up` returns 200. No migration or frontend build was needed. Production debug was disabled and configuration/queue runtime refreshed. Unrelated Social Comments and WhatsApp UI changes remain local. This dirty checkout's local main still starts at `b74d24b`; reconcile the already-deployed narrow patch before the next release without discarding pending work.

2026-09-05 Channel Setup UI: removed the redundant centered “Complete setup with Meta” guide for shared Embedded Signup buttons. Waiting guidance now stays inline in the existing side panel. SDK login, callbacks, cancellation/error handling and Meta's required authorization window are unchanged. Frontend deployment is required; no database migration for this change.

2026-09-05 platform follow-up: added per-provider comment availability to the connection picker, Comments help and mobile JSON. Facebook/Instagram remain the only implemented comment adapters; YouTube/LinkedIn/TikTok are explicitly not integrated. Added unsupported-provider HTTP guards and independent moderation checks. No additional scopes, provider submission, migration or deployment in this follow-up.

2026-09-05: Social Comments is implemented locally behind `SOCIAL_COMMENTS_ENABLED=false`, with a compact Comments page under Social Media Automation, manual/AI replies, reviewed public-KB activation, Meta checks/sync, and mobile JSON endpoints. Requires a new migration and matching frontend plus `social`/`ai` workers. It has not been deployed or submitted to Meta. Public review and external-client tests remain gates; no native mobile UI or full ad discovery is claimed. See `docs/SOCIAL_COMMENTS.md` for exact endpoints, rollout, limitations, and recording checklist. Existing WhatsApp UI edits remain preserved.

Social Comments verification: 74 relevant backend tests passed (319 assertions), including 21 Comments tests; all 28 frontend tests and the production Vite build passed. Changed-file Pint and focused analysis of new Comments services/controllers/jobs/models pass. Repository-wide `composer analyse` remains blocked by existing references to deleted Leads/GooglePlaces/Twitter files. Chrome testing verified the populated desktop detail, explicit disabled public-send controls for an unchecked account, and draft preservation across thread selection; its account serialization regression is covered by an API test. Full mobile/device accessibility and live Meta behavior remain release checks. The new migration was applied only to local MySQL; a preview server runs at `http://127.0.0.1:8011` with the feature enabled for that process. Temporary preview records were removed. No real public reply was sent during local QA.

2026-09-05 follow-up: production release `b74d24b` was deployed as v1.3.38 with its migration, local-built assets, and a dedicated cron-supervised health worker. Operator configuration checks passed; the user's subsequent screenshot confirms live-message processing. Frontend deployment destination is `/home/bluestar/wisperbot.com/public/build`; build locally from the matching source before uploading through cPanel. The current local-only UI follow-up removes webhook details and raw WhatsApp identifiers from client Channel Setup, keeping infrastructure checks in the admin diagnostics and client actions contextual.

Local verification on 2026-09-05: the final connection-health, Embedded Signup, and scheduler-guide run passed 31 tests (87 assertions); frontend tests passed 22 tests, and the production build passed with bundle-size warnings. Changed-file Pint and focused health-code static analysis pass. Repository-wide static analysis remains blocked by existing configuration references to removed Leads/GooglePlaces/Twitter paths. No production migration, worker installation, or live customer verification was performed by this implementation.

WhatsApp connection health and explicit repair are implemented locally behind `CHANNEL_HEALTH_ENABLED=false`. New health snapshots/history require migration, the matching frontend, and a separate `channel-health` worker (120-second timeout; queue retry_after at least 180 seconds). Operator-only rollout uses `CHANNEL_HEALTH_WORKSPACE_IDS`; operator token use additionally requires `META_OPERATOR_BUSINESS_ID` and `META_OPERATOR_WABA_IDS`, verified against Graph ownership. Customer tokens never fall back to the operator token in this flow. Channel Setup distinguishes configuration checks from observed real delivery; Admin → Cron Setup shows platform/worker checks. External customer Cloud API and Coexistence verification remains a deployment gate. No production rollout or version bump is implied by local tests.

Social publishing is consolidated locally under `/app/social/automation`. The unified client page places compact connection management above Upcoming-first post tabs and integrated List/Calendar views; empty workspaces suppress inactive tabs/filters and use concise onboarding rows instead of oversized panels. `/app/social/automation/schedule` uses an explicit Schedule for later or Publish now choice. Legacy accounts/posts/composer/calendar URLs remain compatibility redirects. The i18n endpoint now versions its server cache from locale-file modification times and disables browser caching, preventing newly deployed labels from rendering as raw translation keys. Deploy the matching backend and Vite bundle together.

Guarded Knowledge Base publishing is implemented locally behind `KB_GUARDED_PUBLISHING=false`. It adds revision snapshots, staged extraction/validation/indexing, guided quality review/test/publish UI, exact FAQ and safe answer/query-embedding caches, retrieval diagnostics, knowledge gaps, rollback, and workspace-scoped API actions. Before enabling it, migrate, restart the `ai` worker, rebuild Vite assets, verify migrated revision membership/embeddings, and test that a failed draft leaves the prior live revision unchanged. The legacy retrieval path remains active while the flag is false.

Knowledge Base source authoring now presents URL, File, and Sitemap only. Indexing automatically discovers validated YouTube, Vimeo, and public HTTPS MP4 links in extracted pages/files (including supported iframe and Office hyperlink targets); existing Text, FAQ, and Video records remain compatible. Reindex existing URL/file sources to populate discovered video metadata.

The client label for internal `source_type=sitemap` is now Website. Homepage inputs safely resolve redirects and discover HTML/robots/common sitemap locations; sites without a sitemap fall back to capped same-host links. The production `ai` worker must be restarted after deployment for this behavior.

The Knowledge Base detail screen renders a single active setup/management step rather than stacking every panel. Initial setup gates Define → Sources → Review → Test → Publish; published records default to Monitor and can return to Sources for safe draft changes. Published-source edits, toggles, and reindexing use copy-on-write so the active revision remains immutable; additions and removals affect only draft membership. Automatic publication now requires saved regression tests, preventing a newly indexed source from bypassing the guided client review.

Verification on 2026-09-04: the guarded migration applied locally; the focused AI/KB/API/video suites passed 49 tests (151 assertions), the final guarded/indexing/chatbot/API regression pass passed 12 tests (36 assertions), and `npm run build` succeeded. Targeted changed-file Pint passes. Repository-wide Pint still reports pre-existing formatting in `app/Modules/AI/Services/Llm/GeminiProvider.php`; PHPStan cannot start because `phpstan.neon` contains unmatched ignore paths for removed Leads/Twitter files.

Managed AI credits use each plan's configured `ai_credits_per_month` limit without price-derived defaults. Organization/owner periods, encrypted idempotent ledger replay, workspace provider modes, and provider test state remain in place. Clients see remaining/total credits in the global header and a human-readable fixed-action breakdown on Subscription; successful managed actions charge once, while failures, BYOK, provider tests, and embeddings do not consume credits. Enforcement now defaults on, so production must have a tested managed provider and plan limits verified before deployment. DeepSeek remains restricted to the Super Admin `llm_deepseek_default` integration and is excluded from client provider screens, routes, BYOK resolution, and automatic fallback.

Credit entitlement resolution mirrors billing: an admin-assigned Client subscription takes precedence, then a valid self-service subscription owned by an organization user is pooled at the Client account. This prevents subscribed organization users from incorrectly seeing `0 / 0` while preserving one shared monthly pool.

Active renewable subscriptions now remain credit-eligible when a gateway or legacy admin assignment retains a stale prior-cycle `ends_at` value. The explicit active/trialing status is authoritative; canceled subscriptions still stop receiving credits after their access end. This fixes the Bella Salon & Spa Pro fixture resolving as `0 / 0` despite its configured 100-credit plan allowance.

The managed-credit migration uses explicit `DATETIME` billing-period boundaries for compatibility with MySQL servers that retain legacy implicit `TIMESTAMP` defaults. Its new columns/tables are guarded so a deployment can safely rerun the migration after a partial schema failure.

Alibaba Qwen 3.7 Flash is available locally as the Super Admin-only `llm_qwen_default` managed generation provider. It requires a region-matched Model Studio API key and Workspace ID, must pass its connection test before selection, and does not replace the OpenAI/Gemini embedding path used by Knowledge Bases.

Knowledge Base video answers are now implemented locally. Deployments must run migrations, rebuild the Vite bundle, restart the `ai` queue worker, and verify the customer site's `frame-src` CSP before provider playback can be claimed as operational. Native SDK clients receive the additive `payload.resources` contract but require their own UI release to render the inline card.

Recent work has concentrated on multi-format attachment support (PDFs, Docs, Spreadsheets, Presentations, TXT/CSV, ZIP, Images, Apple HEIC auto-conversion, 10MB limit) and Meta App Review permissions / Facebook & Instagram social publishing behavior:

- unified paperclip attachment button and document cards on Chat Widget and Web Inbox;
- strict 10 MB file validation and MIME normalization across Inbox, Widget, and Mobile APIs;
- automatic Apple HEIC/HEIF photo conversion to JPEG with graceful document fallback;
- channel-level guards preventing document sends over Instagram DM;
- correct OAuth redirects/scopes and publishing controls;

Before changing these flows, read `docs/PRODUCT_DECISIONS.md`, `docs/INTEGRATIONS.md`, the Social controllers/jobs/drivers, and their feature tests.

The current uncommitted work adds WhatsApp Business app Coexistence alongside the existing Cloud API flow. Local tests verify the signup payload, registration skip, sync requests, silent history import, and outbound phone-app echoes. Provider-side verification still requires deploying the matching backend/Vite bundle and completing Embedded Signup with a customer-owned WABA. The Business Portfolio that owns the WisperBot Meta app is not selectable through its own customer Embedded Signup and remains a system-user/operator connection path.

## Immediate technical follow-up

1. After deploying the automation queue normalization, restart workers and perform the one-time legacy `automations` queue drain documented in `docs/OPERATIONS.md`.
2. Keep the production checkout clean; do not overwrite server-only `.env` or uploaded storage.
3. When diagnosing a UI/backend mismatch, verify both `git log -1` and `public/build/manifest.json`.
4. For Meta issues, separate code capability, token/scopes, Page/app subscription, App Review approval, and provider API limitations.
5. Continue adding regression tests for provider-side edit/delete and webhook routing before further production changes.

## Handoff checklist

For the next developer or LLM:

1. Read `AGENTS.md` and `docs/README.md`.
2. Run `git status --short`; preserve unrelated changes.
3. Confirm local `.env` is local-only and contains no production credentials copied into files/logs.
4. Identify the workspace and provider asset ownership path for the feature.
5. Trace controller → service/driver → job/event → model/provider result → UI.
6. Run focused backend/frontend tests and a production build when frontend changes.
7. Update the documentation files required by the impact matrix in the same commit.
8. Record unresolved external/provider dependencies in `KNOWN_ISSUES.md` rather than claiming completion.

## Experimental Smart Bot routing handoff (2026-09-16)

Business-aware Smart Bot answering is implemented on `oris` behind `SMART_BOT_BUSINESS_AWARE_ROUTING=false` by default. It adds answer-scope/fallback/research fields and retrieval-diagnostic metadata in migration `2026_09_16_000200_add_business_aware_answering_to_ai_chatbots`. Local `.env` enables the flag for testing only. Deployments must migrate, build matching Vite assets, clear config caches, and restart AI/message workers. Before promotion, verify greeting, supported business fact, related missing fact, unrelated topic, quick replies, zero-credit greeting, one-credit generation, and approved-source citation behavior in the live widget. The native SDK needs no request change; it may optionally render additive `answer_origin`/`citations`, while Markdown links remain compatible.

## Experimental semantic retrieval handoff (2026-09-16)

Hybrid retrieval is implemented on `oris` behind `KB_HYBRID_RETRIEVAL_ENABLED=false` by default and enabled in local `.env` only. Migration `2026_09_16_000300_add_index_generations_to_knowledge_base_chunks.php` adds index version/generation state and safe diagnostic fields. Local active-first reindex upgraded the internal workspace's Telzen document and website pages to version 2; unrelated sample workspace sources with intentionally invalid local provider credentials failed safely and were not used for validation. The verified internal query `e sim want`, following a greeting, ignores the greeting, selects verified eSIM passages, returns `response_mode=clarification`, and finalizes exactly one `chatbot_reply` credit.

2026-09-16 Telzen follow-up on `oris`: the supplied DOCX was visually and structurally reviewed and confirmed to contain both the China purchase flow and Roman Bangla roaming-data guidance. The failure combined three issues: size chunking separated a later country turn from its purchase instructions, the provider returned useful plain text while the strict runner required JSON, and the local bot had approved-source research disabled with placeholder KB profile fields. Headed multi-turn flows now remain coherent, provider transports request JSON where supported, medium semantic plus strong lexical evidence can answer a clear request, and enabled approved-source research augments purchase/current-information turns. The local KB profile was corrected from the supplied source, research was enabled for bot 2, document 5 was reindexed after restarting the `ai` worker, and the live widget passed both `Want to buy China eSIM` (with approved Telzen citations) and `Kivabe roaming data kinte pari` (Roman Bangla grounded answer). This changes local test data only; no production tenant data was modified.

Deployment requires migration, matching frontend/backend, cache refresh, and `ai`/message worker restart. Run `php artisan ai:kb-reindex-outdated --active-only --limit=100` in bounded batches before enabling the flag. Monitor answer/clarification/fallback rates, repeated clarification, latency, rejected output, and credit refunds. The native SDK contract is additive: existing `body`/`display_body`/`quick_replies` remain authoritative; `response_mode` is optional.
