; Keep PHP's transport limits aligned with the application's 50 MB media rule.
; cPanel PHP-FPM/LSAPI reads per-directory .user.ini settings before Laravel.
upload_max_filesize = 50M
post_max_size = 55M
max_file_uploads = 20
